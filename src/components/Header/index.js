import React from 'react'
import './index.less'

const prefixCls = 'x-header'

const Header = () => (
  <div className={prefixCls}>
    <div className={`${prefixCls}-left`}>
      fds
    </div>
    <div className={`${prefixCls}-right`}>789</div>
  </div>
)

export default Header
